// Where contributors should send their exported file. Fill these in.
// Leave both empty and the page just says "send it to whoever shared this link".
export const CONTACT_LABEL = '';   // e.g. 'yourname@example.com' or 'the ASL club Discord'
export const CONTACT_MAILTO = '';  // e.g. 'yourname@example.com' (adds an email button)
